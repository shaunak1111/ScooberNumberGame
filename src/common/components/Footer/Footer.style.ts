import styled from "styled-components";

export const StyledFooterContainer = styled.footer`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    margin: 2rem 0 0 0;
    padding: 1rem 0.5rem; /* marging Right and margin Left to center the footer buttons */
    justify-content: center;
    button {
        margin: 0 0.5rem; /* To center the footer buttons */
    }
`
